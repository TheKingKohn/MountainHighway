{
  "name": "mountain-highway",
  "version": "1.0.0",
  "description": "A marketplace monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm --prefix packages/api run dev\" \"npm --prefix packages/web run dev\"",
    "build": "npm --prefix packages/api run build && npm --prefix packages/web run build",
    "lint": "npm --prefix packages/api run lint && npm --prefix packages/web run lint",
    "keep-alive": "node keep-alive.js",
    "ping": "node -e \"require('./keep-alive.js').pingAllServices()\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "prettier": "^3.2.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}